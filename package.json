{
  "name": "hello-world-aws",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "run-s clean build:lambdas",
    "build:lambdas": "tsc --build",
    "clean": "rimraf ./bin",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "private": true,
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.303.0",
    "@types/mime": "^3.0.1",
    "aws-sdk": "^2.1348.0",
    "mime": "^3.0.0",
    "rome": "12.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.114",
    "@types/node": "^18.15.11",
    "aws-lambda": "^1.0.7",
    "npm-run-all": "^4.1.5",
    "rimraf": "^4.4.1",
    "typescript": "^5.0.3"
  }
}
